import { SearchBrandPipe } from './search-brand.pipe';

describe('SearchBrandPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchBrandPipe();
    expect(pipe).toBeTruthy();
  });
});
